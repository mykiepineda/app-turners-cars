import BottomNavbar from "./BottomNavbar";
import TopNavbar from "./TopNavbar";

const Footer = () => {
  return (
    <footer>
      <TopNavbar />
      <BottomNavbar />
    </footer>
  );
};

export default Footer;
